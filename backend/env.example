# Copia este archivo como .env en backend/ y ajusta los valores.
# El driver async de SQLAlchemy requiere el prefijo postgresql+asyncpg.
DATABASE_URL=postgresql+asyncpg://admin_netpro:password_seguro@db:5432/netpro_db

# Activa el logging de SQL en consola (0=off, 1=on).
DB_ECHO=0
